#include "main.h"
/**
 * _abs - Comput absolute value of integer
 *
 * @n: an integer number
 *
 * Return: absolute value of a number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-(n));
}
