#include<stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num = 0;
int i;
while (num < 9)
{
i=num+1;
while (i <= 9)
{
putchar(48+num);
putchar(48+i);
if (num != 8)
{
putchar(',');
putchar(' ');
}
i++;
}
num++;
}
putchar('\n');
return (0);
}
