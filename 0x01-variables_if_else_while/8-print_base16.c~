#include<stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num = 0;
char letter = 'a';
while (num < 10)
{
putchar(48 + num);
num++;
}
while (letter <= 'f')
{
putchar(letter);
letter+=1;
}
putchar('\n');
return (0);
}
